<%= form_with(model: student_cohort, local: true) do |form| %>
  <% if student_cohort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_cohort.errors.count, "error") %> prohibited this student_cohort from being saved:</h2>

      <ul>
      <% student_cohort.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="edit-form">
  <div class="edit-section">
    <div class="item-tag">
    <%= form.label :teacher_id %></div>
    <div class="field"><%= form.number_field :teacher_id %>
  </div>
</div>

<div class="edit-section">
  <div class="item-tag">
  <%= form.label :cohort_id %></div>
  <div class="field"><%= form.number_field :cohort_id %>
</div>
</div>

<div class="edit-section">
  <div class="item-tag">
  <%= form.label :teacher_id %></div>
  <div class="field"><%= form.text_field :teacher_id %>
</div>
</div>

<div class="edit-section">
  <div class="item-tag">
  <%= form.label :cohort_id %></div>
  <div class="field"><%= form.text_field :cohort_id %>
</div>
</div>

  <div class="edit-section">
  <div class="actions">
    <%= form.submit %>
  </div>
</div>
</div>
<% end %>
